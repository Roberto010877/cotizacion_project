from django.apps import AppConfig


class ProductosServiciosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'productos_servicios'
    verbose_name = "Gestión de Productos y Servicios"
